{
  "name": "update",
  "scripts": {
    "prebuild": "cp -f ../.src/Eleven.mjs ./src/Eleven.mjs",
    "build": "ncc build index.js -o dist --license licenses.txt --no-cache",
    "test": "node test.js"
  },
  "dependencies": {
    "@actions/core": "^3.0.1",
    "@actions/exec": "^3.0.0",
    "semver": "^7.8.0"
  }
}
